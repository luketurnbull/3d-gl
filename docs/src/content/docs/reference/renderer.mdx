---
title: Renderer
description: Creating a new renderer
---

Create a new WebGL renderer with the Renderer class.

This class accepts a `string` which is the id of a canvas element.

#### Usage

```ts
const renderer = new Renderer("my-canvas-id");
```

import Test from "../../../components/examples/test.astro";

<Test />
